<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Night values overridden to match light theme so app UI remains consistent -->
    <style name="Theme.MedZone" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Keep same colors as the non-night theme to avoid dark-mode inversion -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Disable Android's force-dark auto inversion if present -->
        <item name="android:forceDarkAllowed">false</item>
    </style>
</resources>